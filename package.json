{
  "name": "label-ease",
  "version": "0.1.0",
  "private": true,
  "description": "QR-based inventory tracking for Etsy sellers",
  "workspaces": [
    "web",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web & npm run dev --workspace=backend",
    "build": "npm run build --workspace=web && npm run build --workspace=backend",
    "test": "npm run test --workspace=web",
    "lint": "npm run lint --workspace=web"
  },
  "devDependencies": {}
}
